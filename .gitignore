# Environment / secrets
.env
.env.*
!.env.example
keys/

# Node
node_modules/
.next/
dist/
*.tsbuildinfo

# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/
.ruff_cache/

# Databases / dumps
*.sqlite
*.sqlite3
*.dump

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Logs
*.log

# Example env files are documentation, not secrets
!.env.prod.example

# vim swap files (a .env swap once nearly leaked into a commit)
*.swp

# Python build artifacts (sdk-python)
build/
*.egg-info/

# Local console screenshots — not referenced by any doc, and swept in twice
# by "git add docs". Ignored so it cannot happen a third time.
docs/console-*.png
