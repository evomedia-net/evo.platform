# Deploying the EvoPlatform service

This content has moved into the customer-facing guide:

**→ [Self-hosting EvoPlatform — Part B: Put it on a server](guides/01-self-hosting-evoplatform.md#part-b-put-it-on-a-server)**

The guide covers the same ground — hostname choice, the production compose
stack, nginx and TLS, DNS cutover — plus backups, updating, rollback, and
troubleshooting, written for someone who has not deployed this before.

It lives with the rest of the [customer guides](guides/), so deploying the
platform and adding Ask AI read as one sequence rather than two unrelated
documents.
